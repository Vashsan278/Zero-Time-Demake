/// tb_SetMouseBinding( use, advance, showHide );
/*
    Description:
        Map the mouse to the dialogue.
    Arguments:
        - use:      True or False, weither to use or not the mouse
        - advance:  Mouse button to advance in the messages
        - showHide: Mouse button to show or hide the dialogue
    Returns: Nothing
    NOTE: By default, mouse is disabled ( use = false )
*/
useMouse = argument[0];
mAdvance = argument[1];
mShowHide = argument[2];
