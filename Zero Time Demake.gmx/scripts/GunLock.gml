///GunLock
ds_list_add(obj_controller.characterOrder, "sCharNarrator");
tb_AddMessage(                
"Diana: #It says 'STOP' here, so maybe#it'll stop the incineration process!"
);

ds_list_add(obj_controller.characterOrder, "sCharNarrator");
tb_AddMessage(
"Sigma: #Hmm, if it does then that's#one less thing to worry about. Let's#find the ley that fits this."
);

ds_list_add(obj_controller.characterOrder, "sCharNarrator");
tb_AddMessage(
"Diana: #We'd definately fee; calmer#once Phi is safe. Wait a second...#Isn't this keyhole too big?"
);
